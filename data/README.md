# Data Directory

This project keeps only small demo-safe samples in Git. Large raw extracts, local downloads, and generated analytical stores should stay out of source control.

Use `data/sample` for tiny open-data-compatible fixtures and `data/external` for ignored local datasets.

